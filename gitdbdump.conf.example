# Which database-systems should be backuped? Options are "mysql", "postgres" and "ejabberd" at the moment.
#  MySQL needs .my.cnf to be configured
#  Postgres needs the user which runs the backupscript to be a postgres-superuser. You can make root a postgres-superuser by issuing "createuser root"
databases="mysql postgres"

# Backup-Dir
backup_basedir="/var/backups/dbdumps.git"

# Permissions for backup-archive
backup_user="root"
backup_permissions="600"
